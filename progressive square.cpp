#include <bits/stdc++.h>

using namespace std;

bool isPresent(int arr[], int size, int target) {
    for (int i = 0; i < size; ++i) {
        if (arr[i] == target) {
            return true;  // If target element found, return true
        }
    }
    return false;  // If target element not found, return false
}

int main() {
    int n=0,c=0,d=0;


    return 0;
}
